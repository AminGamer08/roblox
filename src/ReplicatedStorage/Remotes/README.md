This folder is a placeholder for RemoteEvents and RemoteFunctions created in Studio.

`ServerScriptService/RemotesSetup.server.lua` will ensure the required RemoteEvents (`TrendingUpdated`, `ChallengeState`, and `RewardGranted`) exist at runtime, but keeping this folder in source control makes it clear where the instances belong when working in Studio.
